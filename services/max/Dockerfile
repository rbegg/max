# Use a minimal base image to keep the placeholder lightweight.
FROM alpine:latest

# When the container starts, run this command to keep it alive indefinitely.
# This is a good practice for placeholders or for services that you might
# want to attach a shell to for debugging.
CMD ["sleep", "infinity"]
